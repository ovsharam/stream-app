# Generated by: npm run setup:stream-tunnel
# Permanent tunnel — api.appliedscope.com → local STREAM API (:3131)

tunnel: {{TUNNEL_ID}}
credentials-file: {{CREDENTIALS_FILE}}

ingress:
  - hostname: api.appliedscope.com
    service: http://localhost:3131
  - service: http_status:404

services:
  falkordb:
    image: falkordb/falkordb:latest
    ports:
      # 6380 avoids conflict with brew redis on 6379
      - '6380:6379'
    volumes:
      - falkordb_data:/data

volumes:
  falkordb_data:

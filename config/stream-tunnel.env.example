# Copy to config/stream-tunnel.env (gitignored) — filled by npm run setup:stream-tunnel
TUNNEL_NAME=stream-api
TUNNEL_ID=
CREDENTIALS_FILE=
PUBLIC_HOST=api.appliedscope.com
STREAM_API_PUBLIC_URL=https://api.appliedscope.com
